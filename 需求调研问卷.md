# SRT Flow 需求调研问卷

为了更好地完成 SRT Flow 项目的开发，请您协助确认以下细节。可以在选项后的 `[ ]` 中打 `x` 表示选中，或在横线上填写内容。

## 1. 技术选型与架构

- **前端框架**：系统提示建议默认使用 Vanilla JS/CSS。考虑到本项目交互较多（任务管理、进度轮询、预览编辑），您是否接受使用轻量级框架（如 Vue.js 或 React）以提高开发效率和维护性，还是坚持使用原生技术？
  - [ ] 坚持原生 (Vanilla JS/HTML/CSS) - *适合极简依赖*
  - [x] 接受 Vue.js (推荐，上手快，组件化开发，适合此类管理工具)
  - [ ] 接受 React
  - [ ] 其他：______

- **后端框架**：Python 3.12 环境下，您偏好哪种 Web 框架？
  - [x] FastAPI (推荐，高性能，原生支持异步，适合长耗时任务接口)
  - [ ] Flask (轻量，简单)
  - [ ] Django (全功能，但可能对本项目略显厚重)

- **任务队列**：
  - 考虑到“任务重启”和“排队”需求，任务队列是否需要持久化（重启服务后任务不丢失）？
    - [x] 是，需要持久化 (将使用 SQLite 或文件存储队列状态)
    - [ ] 否，内存队列即可 (重启服务后队列清空)

## 2. 功能细节

### 2.1 视频下载
- **认证支持**：是否需要支持需要登录才能观看的视频（如 YouTube Premium 内容或 B 站大会员内容）？
  - [x] 不需要，仅下载公开视频
  - [ ] 需要 (需提供 Cookie 导入/管理功能)

### 2.2 语音识别 (ASR)
- **Whisper 模型**：本地 Whisper 默认使用哪个模型大小？(模型越大准确率越高，但速度越慢且吃资源)
  - [ ] tiny/base (快，精度一般)
  - [ ] small/medium (平衡)
  - [x] large-v3 (高精度，需较好硬件)
  - [ ] 用户可在界面配置 (默认设置为：______)

### 2.3 语音合成 (TTS)
- **TTS 引擎**：您计划使用哪种 TTS 方案？
  - [ ] Edge-TTS (免费，效果不错，在线)
  - [ ] OpenAI TTS / Azure TTS (付费，效果极佳，需 API Key)
  - [x] 本地 TTS (如 Coqui TTS / ChatTTS，吃显卡资源)
  - [ ] 系统内置/操作系统 TTS

### 2.4 视频合成
- **字幕样式**：对于“可配置的样式”，需要支持到什么细粒度？
  - [x] 基础 (字体、大小、颜色、位置)
  - [x] 进阶 (边框、阴影、背景色、动画效果)

- **音频处理**：替换翻译语音时，原音如何处理？
  - [x] 完全静音
  - [ ] 背景音保留 (Ducking/闪避效果，原音降低音量)
  - [ ] 可配置

### 2.5 素材生成
- **缩略图生成**：是指利用 LLM 生成“提示词”然后调用绘图 AI，还是仅截取视频帧？
  - [ ] 仅截取视频帧
  - [ ] LLM 生成提示词 + 调用绘图接口 (需确认绘图服务提供商，如 OpenAI DALL-E)
  - [x] 其它：用户可以选择是截取视频帧还是LLM生成提示词+调用绘图接口（绘图接口可配置）

## 3. 部署与运维

- **Docker**：是否需要提供 `docker-compose.yml` 一键部署文件？
  - [x] 是
  - [ ] 否

- **端口**：默认服务端口是否有偏好？
  - [ ] 默认 (如 8000)
  - [x] 指定：8010

## 4. 其他

- **UI 风格**：是否有偏好的设计风格？
  - [ ] 极简/黑白
  - [x] 现代/科技感 (深色模式)
  - [ ] 柔和/明亮
  - [ ] 参考某个网站：______

- **补充需求**：
  - (请在此处填写您想到的其他补充点)
