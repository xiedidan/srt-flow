# SRT Flow Backend Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25
aiosqlite>=0.19.0

# Validation & Serialization
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP Client
httpx>=0.26.0
aiohttp>=3.9.0

# Security
cryptography>=41.0.0
python-jose[cryptography]>=3.3.0

# Configuration
python-dotenv>=1.0.0

# WebSocket
websockets>=12.0

# Utilities
python-dateutil>=2.8.2
aiofiles>=23.2.0
py7zr>=0.20.0

# External Tools Integration
yt-dlp>=2024.1.0
# ffmpeg - system dependency (install via system package manager)

# AI/ML Dependencies (optional, for ASR/TTS)
# openai-whisper - installed separately with GPU support
# torch - installed separately with CUDA support

# TTS Engines (optional, install as needed)
# ChatTTS>=0.1.0  # pip install ChatTTS
# TTS>=0.22.0  # Coqui TTS: pip install TTS
# IndexTTS: git clone https://github.com/index-tts/index-tts && cd index-tts && pip install -e .
# SparkTTS: git clone https://github.com/SparkAudio/Spark-TTS && cd Spark-TTS && pip install -e .
# CosyVoice: git clone https://github.com/FunAudioLLM/CosyVoice && cd CosyVoice && pip install -e .

# Online TTS Services
edge-tts>=6.1.0  # Microsoft Edge TTS (free online service)
# Azure TTS and Volc TTS use httpx (already included above)

# Model Download
huggingface_hub>=0.20.0
